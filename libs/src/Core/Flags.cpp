#include <Flags.hpp>
#include <QFile>

bool lineNumbers() {
    
    return true;
}

bool removeChars(char character) {

    return true;
}

bool removeWord(const QString & word) {
    
    return true;
}